package misc.binarySearch;

/**
 * array is infinite
 * size = infinite
 * where to mark the high index ?
 * keep the end index at 1
 * now, compare the arr[end] and element.
 * If the element is greater than end then inc the end = end * 2
 * start = old end
 * stop this when arr[end] > element
 * This gives the start and end index
 * Now, do the classic binary search
 * */
public class PositionOfAnElementInInfiniteSortedArray {

    public static void main(String[] args) {


    }
}
